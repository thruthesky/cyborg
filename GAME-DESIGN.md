# Cyborg — 게임 설계 문서

> AI 로봇에게 점령된 세계를, 인간 사이보그가 되찾는 2.5D 아이소메트릭 액션 MMORPG.

이 문서는 **지금 이 저장소가 무엇을 만들고 있는지**를 담는다. 세계관과 톤,
기술 구조, 핵심 로직의 동작 원리, 그리고 목표 대비 현재 위치를 기록한다.
수치와 파일 경로는 코드에서 직접 확인한 실제 값이다.

- 최종 갱신: 2026-08-04
- 대상 코드: Dart 약 15,700줄 (`lib/`) + Rust 약 1,830줄 (`spacetimedb/src/`)

---

## 1. 한눈에 보기

| 항목 | 내용 |
|---|---|
| 장르 | 2.5D 아이소메트릭 액션 RPG / 공유 오픈월드 MMORPG |
| 시점 | 2:1 다이메트릭 아이소메트릭, 화면 정렬(빌보드) 캐릭터 |
| 플랫폼 | Flutter (macOS · iOS · Android · Web) |
| 엔진 | Flame 1.38 |
| 백엔드 | SpacetimeDB 2.7 (Maincloud), Rust 모듈 |
| 월드 | 걸어서 1 km × 1 km 단일 공유 맵 (격자 1006 × 1006 타일) |
| 아트 | **에셋 없는 전량 절차적 렌더링** — 모든 캐릭터·지형·이펙트를 Canvas로 그린다 |
| 오디오 | 효과음 40여 종 + BGM (`assets/audio/`) |

**핵심 루프**
> 안전지대에서 출격 → 데이터 공간을 돌아다니며 상주 로봇 사냥 → 전리품 회수·레벨업
> → 더 깊은 구역으로 나갔다 몸체 파괴 → 안전지대에서 재가동(성장은 유지) → 다시 출격

이 루프에는 끝도 초기화도 없다. 판을 나누는 단위가 없으므로 "클리어" 라는 상태도
없다.

---

## 2. 세계관

### 2.1 전제

AI 로봇이 점령한 것은 물리적인 도시가 아니라 **세계를 굴리는 전산망 그 자체**다.
인류는 그 안으로 의식을 실어 보낼 수 있는 군용 사이보그 프레임을 만들었고,
플레이어는 그 프레임을 입고 데이터 공간에 **침투(incursion)** 한다.

이 설정은 두 가지 게임 규칙을 자연스럽게 정당화한다.

1. **죽음이 끝이 아니다.** 파괴되는 것은 몸체이고 의식은 백업되어 있다.
   그래서 사망은 런의 종료가 아니라 **안전지대로의 강제 귀환**이며,
   레벨·경험치·버프는 그대로 유지된다 (`Player.respawnAt`).
2. **모두가 같은 공간에 있다.** 인스턴스나 매치가 아니라 하나의 전산망이므로,
   플레이어는 세션에 들어가는 게 아니라 **월드에 접속한다**.

### 2.2 비주얼 톤 — "빛으로 가득 찬 사이버 스페이스"

흔한 사이버펑크의 어두운 폐허를 의도적으로 뒤집었다. 무대는 **눈부시게 밝은
데이터 공간**이다. 흰빛에 가까운 데이터 플레이트 위로 청록(아군)과
마젠타(적)의 발광이 흐른다.

| 진영 | 색 | 의미 |
|---|---|---|
| 플레이어 | 청록 `#00E5FF` | 에너지 블레이드, 바이저, 코어 |
| AI 로봇 | 마젠타 `#FF1F6B` | 센서 아이, 플라즈마, 경고 표시 |
| 지형 | 흰빛~옅은 청록 | 데이터 플레이트, 도관, 타워 |
| 방화벽 | 핑크 `#FF2E77` | 밟으면 피해를 주는 위험 구역 |

밝은 배경에서 캐릭터 실루엣이 죽지 않도록 **본체는 짙게, 발광은 강하게** 유지한다.
그림자도 검정이 아니라 푸른 기가 도는 반투명(`#552A5C7A`)이다.

전체 팔레트: `lib/game/palette.dart`

### 2.3 용어

| 용어 | 뜻 |
|---|---|
| 데이터 공간 | 게임 월드. AI가 점령한 전산망 내부 |
| 요원 / 사이보그 | 플레이어 캐릭터 |
| 침투(incursion) | 한 번의 플레이 세션 |
| 데이터 플레이트 | 일반 바닥 타일 |
| 도관 / 스트림 | 통행을 보장하는 대로. 스트림은 더 넓은 간선 |
| 방화벽 구역 | 지속 피해를 주는 위험 타일 |
| 데이터 타워 | 벽 구조물 (파괴 불가) |
| 데이터 캐시 | 파괴 가능한 상자 |
| 재가동 | 몸체 파괴 후 안전지대에서 복귀하는 것 |

---

## 3. 기술 아키텍처

### 3.1 레이어

```
┌─────────────────────────────────────────────────────────┐
│  앱 셸           main.dart · CyborgApp                    │
│  관문            auth/cyborg_gate.dart                    │
│    연결 → 로그인 → 캐릭터 선택 → 출격                       │
├─────────────────────────────────────────────────────────┤
│  게임 본체       game/action_rpg_game.dart (FlameGame)     │
│    ├ 월드        level/  (지형·청크·안전지대)               │
│    ├ 엔티티      entities/ (플레이어·적·구조물·전리품)        │
│    ├ 시스템      systems/ (레벨·드롭·버프·인벤토리·개체군)     │
│    ├ 연출        fx/ · audio/                             │
│    └ UI          ui/ (HUD·메뉴·패널)                       │
├─────────────────────────────────────────────────────────┤
│  동기화          game/net/game_sync.dart (인터페이스)        │
│                  game/net/spacetime_game_sync.dart        │
├─────────────────────────────────────────────────────────┤
│  SpacetimeDB     spacetime/ (연결·생성 바인딩)              │
│  클라이언트       spacetimedb_sdk 2.4                       │
├═════════════════════════════════════════════════════════┤
│  서버 모듈       spacetimedb/src/ (Rust → wasm)            │
│    lib.rs (계정·세션·캐릭터) · auth.rs · character.rs        │
│    leaderboard.rs · world.rs (몬스터·킬 판정)               │
│  DB: withcenter-cyborg @ maincloud.spacetimedb.com       │
└─────────────────────────────────────────────────────────┘
```

### 3.2 설계 원칙

**게임은 서버 없이도 돈다.** 동기화는 `GameSync` 추상 클래스 뒤에 있고,
서버가 없으면 기본 구현이 아무것도 하지 않는다. 리더보드도
`LeaderboardSource`가 비어 있으면 "연결되지 않았다"고만 표시하고 게임은 그대로
진행된다. 오프라인 진입점(`lib/offline_main.dart`)도 따로 있다.

**클라이언트를 신뢰하지 않는다.** 서버 reducer는 `account_id`를 인자로 받지
않는다. 소유자는 언제나 `ctx.sender()`에서 도출한 세션으로만 정한다.

**하나의 출처.** 캐릭터 외형 수치는 `CyborgDesign` 한 곳에만 있고, 선택 화면과
인게임이 같은 렌더러(`CyborgRenderer`)를 쓴다. 고른 몸과 조종하는 몸이
어긋날 수 없다.

---

## 4. 좌표계와 렌더링

### 4.1 세 개의 좌표계

`lib/game/iso.dart`가 전부 정의한다.

| 좌표계 | 단위 | 쓰는 곳 |
|---|---|---|
| **그리드** | 타일 (= 1 m) | 모든 게임 로직 — 이동 속도, 사거리, 시야, 충돌 |
| **월드(화면)** | 픽셀 | Flame 컴포넌트의 `position` |
| **뷰포트** | 픽셀 | HUD, 터치 컨트롤 |

핵심 변환 (2:1 다이메트릭, 타일 128 × 64 px):

```dart
screenX = (gx - gy) * 64          // kHalfTileWidth
screenY = (gx + gy) * 32 - z * 56 // kHalfTileHeight, kHeightUnit
```

`z`는 고도다. 드론이 떠 있는 높이, 구조물의 층수, 전리품의 튀어오름이 모두
같은 축을 쓴다. 이것이 이 게임을 "2D"가 아니라 **2.5D**로 만든다.

**게임 수치는 전부 타일 단위**라는 규칙이 중요하다. `kMetersPerTile = 1.0`이므로
"이동 속도 3.6"은 곧 초속 3.6 m다. 밸런스를 픽셀이 아니라 물리적 감각으로 다룰 수 있다.

### 4.2 깊이 정렬

아이소메트릭에서는 `x + y`가 클수록 화면 앞쪽이다. Flame의 `priority`가
정수라 소수점을 보존하기 위해 100배한다.

```dart
priority = ((grid.x + grid.y + bias) * 100).round()
```

`bias`로 같은 칸 안의 순서를 미세 조정한다. 구조물은 `-0.05`(뒤로),
플레이어는 `+0.02`, 이펙트와 데미지 텍스트는 `+3 ~ +4`(항상 앞).

모든 월드 오브젝트는 `IsoEntity`(`entities/iso_entity.dart`)를 상속하고,
매 프레임 `syncTransform()`이 그리드 좌표를 화면 좌표와 priority로 반영한다.

### 4.3 캐릭터는 빌보드다

지형은 아이소메트릭으로 눕혀 그리지만 **캐릭터는 화면에 정면으로 세워** 그린다.
원점은 발밑, y축은 위로 갈수록 음수. 방향은 8방향을 두 개의 불리언으로 압축한다.

- `facesRight(dir)` → 좌우 반전 (`canvas.scale(-1, 1)`)
- `facesDown(dir)` → 앞모습(바이저 보임) / 뒷모습(동력팩 보임)

### 4.4 전량 절차적 아트

**이미지 에셋이 하나도 없다.** 캐릭터, 로봇, 지형, 이펙트 전부 `Canvas` 명령으로
그린다. 그 대가로 얻은 것:

- 캐릭터 체형을 수치로 정의 → 새 캐릭터 추가가 렌더링 코드 수정 없이 가능
- 해상도 독립적, 앱 용량이 거의 0
- 색·발광을 런타임에 자유롭게 조절 (피격 플래시, 버프 틴트)

관련 파일: `entities/cyborg_design.dart`(수치), `entities/cyborg_renderer.dart`(그리기),
`entities/enemy.dart`(로봇 4종), `entities/block.dart`(큐브), `fx/`(이펙트).

---

## 5. 월드

### 5.1 규모와 스트리밍

```
걸을 수 있는 영역  1000 × 1000 타일 = 1 km²      ← 이것이 규격이다
외곽 허공 테두리   3타일 (양쪽, 통행 불가)
격자             1006 × 1006 타일 = 약 101만 칸
청크             32 × 32 타일 → 32 × 32 = 1024 청크
```

**규격은 격자 크기가 아니라 "플레이어가 실제로 걸을 수 있는 거리"다.** 아이소메트릭
에서 바닥이 그냥 잘리면 어색해 월드 가장자리를 허공으로 두르는데, 그 테두리를 격자
안쪽에서 떼어 오면 걸을 수 있는 거리가 그만큼 줄어든다. 그래서 테두리는 1 km **바깥에**
덧붙인다 — 격자가 1006인 이유가 이것이다.

상수는 명세에서 유도된다 (`iso.dart`). `kWorldSizeMeters` 하나만 고치면 나머지가 따라온다.

```dart
kWorldSizeMeters (1000.0 m)  ← 규격
  → kWorldPlayableTiles = kWorldSizeMeters ~/ kMetersPerTile   // 1000
    → kWorldTiles = kWorldPlayableTiles + kWorldEdgeMarginTiles * 2   // 1006
```

100만 칸을 객체로 들 수 없으므로 지형은 **평면 배열**에 담는다
(`Uint8List` 3개: 타일 종류, 구조물 높이, 구조물 종류 — 합계 약 3 MB).
구조물 객체(`BlockSpec`)는 청크를 조회할 때만 즉석에서 만든다.

서버도 같은 값을 들고 있으며(`world.rs`의 `WORLD_PLAYABLE_TILES`·`WORLD_EDGE_MARGIN`·
`WORLD_TILES`), 손으로 맞춘 값이므로 **서버 테스트가 클라이언트 값과 대조한다** —
클라이언트 테스트는 자기 파생끼리만 비교해 이 어긋남을 잡지 못하기 때문이다.

`lib/game/level/level_map.dart`

**3단계 스트리밍**이 돌아간다.

| 대상 | 주기 | 범위 | 방식 |
|---|---|---|---|
| 지면 | 매 프레임 | 화면 + 여유 2타일 | 청크를 `ui.Picture`로 구워 캐시 (최대 96개, LRU) |
| 구조물 | 0.25초 | 화면 + 여유 16타일 | 청크 단위로 `BlockComponent` 마운트/회수 |
| 로봇 | 0.3초 | 반경 46 m 활성 / 60 m 해제 | 장부(`MonsterSeed`) ↔ 컴포넌트(`Enemy`) 전환 |

지면 청크는 **프레임당 최대 3개만** 새로 굽는다. 빠르게 이동할 때 프레임
스파이크가 생기지 않도록 한 예산이다 (`ground_layer.dart`).

구조물 여유가 16타일로 넉넉한 이유는 높은 데이터 타워가 **화면 아래 경계 밖에서도
위로 솟아 보이기** 때문이다. 아이소메트릭 특유의 함정이다.

### 5.2 지형 생성 — 3층 구조

`LevelMap.generate()`가 시드 하나로 100만 칸을 만든다.

**1층 — 데이터 공백**
저해상도 값 노이즈 두 겹(큰 덩어리 + 잔결)을 보간해 값이 낮은 곳을 허공으로
뚫는다. 잔결이 섬 가장자리를 너덜너덜하게 만들어 인공적인 느낌을 없앤다.

**2층 — 도관 대로 격자**
40타일 간격으로 폭 5칸의 도관 대로를 가로세로로 깐다. 다섯 줄에 하나는 폭 9칸의
고속 스트림 간선으로 승격한다. **이 격자가 통행 가능성을 보장한다** — 노이즈가
어떻게 나오든 대로는 항상 연결되어 있다.

**3층 — 구획 내부**
대로가 나눈 구획 안에 데이터 타워(속이 빈 껍데기 = 엄폐물), 데이터 캐시,
방화벽 구역을 배치한다.

### 5.3 안전지대

월드 정중앙 **50 m × 50 m** 정사각형 (`level/safe_zone.dart`).

- 적은 경계 안으로 들어올 수 없다 (몸통이 걸치는 것도 차단)
- 안에 있는 플레이어는 공격받지 않는다 — 추격 중이던 적도 어그로가 풀린다
- 예비 동작 중에 플레이어가 피해 들어가면 공격을 거둔다
- 몸체 파괴 시 여기서 재가동한다
- 상주 개체는 경계에 몸통이 걸치는 자리에도 놓이지 않는다

배치는 월드를 만들 때 한 번 정해지고 리스폰도 제자리에서 일어나므로, 안전지대가
뚫릴 경로는 배치 단계 하나뿐이다. `test/safe_zone_test.dart`가 안전지대 둘레를 훑어
한 마리도 들어와 있지 않은지 지킨다.

렌더링은 지면 위·캐릭터 아래에 깔리는 역장 벽이며, 정적인 부분은 `ui.Picture`로
굽고 맥동 발광만 매 프레임 덧그린다.

---

## 6. 캐릭터

### 6.1 두 개의 프레임

| 프레임 | 서버 ID | 컨셉 |
|---|---|---|
| `assault` (남성형) | `male_cyborg` | 강습 — 정면 화력과 방어 |
| `infiltrator` (여성형) | `female_cyborg` | 침투 — 기동과 은신 |

외형은 `CyborgDesign`이 픽셀 단위 수치로 정의한다: 총 높이, 어깨 폭, 흉곽 폭,
허리 폭, 골반 폭, 팔다리 두께, 목 길이 등. 골격 차이를 그대로 반영해
**실루엣만으로 구분**된다.

서버 ID 문자열은 `spacetimedb/src/character.rs`의 `CHARACTER_KINDS` 화이트리스트와
**글자 그대로** 일치해야 한다. 클라이언트 UI가 두 가지만 보여준다는 사실을
서버가 믿지 않기 때문이다.

`lib/auth/cyborg_kind.dart`가 서버 문자열 ↔ 프레임 대응만 담당한다 (수치는 갖지 않음).

### 6.2 기본 스탯

| 스탯 | 초기값 | 비고 |
|---|---|---|
| 체력 | 120 | |
| 에너지 | 100 | 초당 16 자동 회복 |
| 근접 피해 | 26 | 3타 콤보 마무리 시 ×1.6 |
| 원거리 피해 | 18 | 발사당 에너지 12 |
| 이동 속도 | 3.6 타일/초 | |
| 전리품 흡입 반경 | 2.8 타일 | |

버프가 걸리면 `effectiveMeleeDamage` 등 `effective*` 게터를 통해 배율이 곱해진다.

---

## 7. 전투

### 7.1 플레이어 행동

**근접 — 에너지 블레이드 (0.32초)**
3타 콤보. 콤보 창은 0.65초. 스윙 중반(진행도 35%)에 **한 번만** 판정하며,
전방 약 120도 부채꼴 × 사거리 1.5타일 안의 대상을 모두 때린다.
마무리 3타는 피해 ×1.6, 넉백 강화, 치명타 표시. 명중 시 에너지 4 회복.

**원거리 — 플라즈마 볼트**
에너지 12 소모, 쿨다운 0.24초. 직선 발사체(`Projectile`).

**회피 — 대시 (0.18초)**
에너지 20, 쿨다운 0.9초. 속도 13 타일/초, **대시 중 + 0.08초 무적**,
청록 잔상을 남긴다.

**포션**
`Q`로 가장 아깝지 않은 회복 포션 자동 사용, `1`~`6`으로 퀵슬롯 지정 사용.

### 7.2 이동과 충돌

축을 분리해 이동시켜 **벽을 따라 미끄러진다**.

```dart
if (delta.x != 0 && free(grid.x + delta.x, grid.y)) grid.x += delta.x;
if (delta.y != 0 && free(grid.x, grid.y + delta.y)) grid.y += delta.y;
```

`free()`는 몸 반경의 네 모서리가 모두 통행 가능한지 본다. 원-사각형 정밀
판정 대신 이 근사를 쓰는 이유는 타일 기반 월드에서 충분히 정확하고 훨씬 싸기 때문이다.

넉백은 속도에 더해진 뒤 지수적으로 감쇠한다 (`1 - dt * 9`).

### 7.3 피격과 사망

- 피격 시 0.55초 무적 + 깜빡임, 화면 흔들림, 붉은 비네트
- 방화벽 구역은 0.5초마다 5 피해 (무적 무시)
- 체력 0 → **몸체 파괴**. 안전지대에서 재가동, 2초 무적
- **레벨·경험치·버프는 유지된다.** 사망은 위치 이동이지 진행 초기화가 아니다
- `deaths` 카운터만 올라간다

재가동은 **전투 상태를 남김없이 되돌린다** — 속도·넉백·잔상·쿨다운·진행 중이던
클릭 이동 목표까지 전부 버린다. 하나라도 남으면 새 몸체가 죽은 자리의 관성을 이어받는다.
특히 클릭 이동 목표를 남기면 아무도 조작하지 않았는데 죽은 자리(1 km 월드에서는 수백 m
밖)를 향해 혼자 안전지대를 걸어 나가고, 무적이 끝나는 순간 다시 죽는다. 텔레포트도 같다.

### 7.4 타격감 장치

| 장치 | 값 |
|---|---|
| 히트스톱 | 일반 처치 0.05초, 보스 0.16초 (`dt *= 0.25`) |
| 화면 흔들림 | 근접 명중 4, 콤보 마무리 8, 피격 9, 보스 폭발 26 |
| 데미지 텍스트 | 떠오르며 페이드, 치명타는 크기 맥동 |
| 히트 스파크 | 방사형 파편 8개 + 중심 섬광 |
| 폭발 | 충격파 링(눌린 타원) + 화염 코어 + 회전하는 파편 |

---

## 8. 적 — AI 로봇

### 8.1 4개 골격 계통 × 200종 도감

몬스터는 고정된 4기종이 아니라 **레벨 1~200에 한 종씩** 대응하는 도감이다
(`monster_codex.dart`의 `MonsterCodex`). 20개 계열 × 10개 등급으로 200종이 채워지며,
`레벨 = 등급 × 20 + 계열 + 1`이라 어떤 레벨에도 정확히 한 종이 있다.

골격 계통(`MonsterBuild`)은 실루엣과 전투 성향을 정한다.

| 계통 | HP 배율 | 속도 | 사거리 | 특징 |
|---|---|---|---|---|
| `drone` 비행 | ×0.72 | 2.9 | 1.0 | 공중 부양(z=0.85), 가장 멀리서 반응 |
| `walker` 보행 | ×1.15 | 2.1 | 1.25 | 2족 보행, 근접 강타. 코앞까지 와야 알아챈다 |
| `siege` 포격 | ×1.6 | 1.35 | 7.5 | 3연발 플라즈마, 크기 1.25배 |
| `sovereign` 지휘 | ×2.8 | 1.6 | 9.0 | 구역 보스. 5발 산탄 + 유도, 크기 1.9배 |

**공격력은 계통과 무관하게 그 종의 레벨과 같다** — 레벨 3 몬스터는 3, 레벨 200은 200을
준다(§8.5). 체력·속도·사거리·연발 수만 계통별로 다르다.

넉백 저항도 계통별로 다르다 (비행 1.4배 잘 밀림 ↔ 지휘 0.12배).

### 8.2 AI 상태 기계

```
idle ──(어그로 범위 안)──▶ chase ──(사거리 도달)──▶ telegraph
 ▲                          │                            │
 └──(어그로 상실/안전지대 진입)┘                    (예비 동작 종료)
                                                          ▼
                              recover ◀──(공격 종료)── strike
                                │
                                └──▶ chase
```

**어그로 범위는 1~5 m다.** 계통마다 구간이 다르고 그 안에서 **개체마다 무작위로**
정해지므로, 같은 기종이라도 반응 거리가 조금씩 다르다.

| 계통 | 어그로 구간 |
|---|---|
| `drone` 비행 | 4.0 ~ 5.0 m — 센서 특화, 가장 멀리서 반응 |
| `walker` 보행 | 1.0 ~ 2.5 m — 둔하다. 코앞까지 와야 알아챈다 |
| `siege` 포격 | 2.5 ~ 4.0 m — 사거리는 길지만 탐지는 중간 |
| `sovereign` 지휘 | 4.5 ~ 5.0 m — 상한까지 예민하다 |

어그로 범위가 사거리보다 짧은 계통(`siege` 7.5 m, `sovereign` 9.0 m)은 **먼저 알아챈
뒤에야 쏜다.** 사거리 안에 들어왔다고 자동으로 공격이 시작되지는 않는다.

추격을 포기하는 거리는 **어그로 + 2.5 m**다. 경계에서 추격/배회가 매 프레임 뒤집히지
않도록 둔 여유이며, 플레이어가 안전지대로 들어가면 거리와 무관하게 즉시 풀린다.

**`telegraph`(예비 동작)가 이 게임 전투의 핵심이다.** 공격 직전 0.32~0.75초 동안
지면에 경고 표시가 뜬다 — 근접형은 사거리 원(아이소 타원), 원거리형은 조준선.
플레이어는 이 창에서 대시로 피하거나 안전지대로 물러날 수 있다.

배회 중에는 1.5~4초마다 반경 1.5~4.5타일 안의 새 목표를 잡는다.
이동 시 **분리(separation) 스티어링**으로 서로 겹치지 않는다.

**계통마다 다르게 움직인다** (`systems/enemy_tactics.dart`). 예전에는 모든 기종이
일직선으로 걸어와 그 자리에 못 박힌 채 때렸다 — 몸집과 사거리는 달라도 움직임이
같으니 교전의 모양이 하나뿐이었다.

| 계통 | 추격 | 공격 직후(`recover`) |
|---|---|---|
| 비행 `drone` | 36°만큼 비껴 돌며 파고든다(개체마다 도는 쪽이 갈린다) | 사거리 밖이면 다시 붙는다 |
| 보행 `walker` | 곧장 파고든다 | 사거리 밖이면 다시 붙는다 |
| 포격 `siege`·지휘 | **사거리의 55%보다 가까우면 물러난다** | 붙어 있으면 물러난다 |

원거리 기종은 그 거리 안에서는 공격도 시작하지 않는다. 붙어서 쏘는 포격 기체가
근접 무기의 밥이라는 것이 이 계통의 약점이고, 플레이어가 파고들 이유다.
경직 구간의 이동은 제 속도의 45%다.

**경보는 번진다.** 한 기가 플레이어를 발견하거나 **뒤에서 맞으면**, 반경 4.5 m
안의 잠든 동료가 함께 깨어난다(경계 지휘 정예는 12 m). 깨우는 것은 어그로
상태뿐이며 경보를 들은 쪽은 다시 소리치지 않는다 — 그러지 않으면 한 번의 발견이
월드를 타고 번진다. 사거리 밖에서 하나씩 저격해 무리를 지우는 길을 막는 장치다.

### 8.3 개체군 — 장부와 실체

월드에 로봇 수천 기가 상주한다. 전부 컴포넌트로 들 수 없으므로
**장부(`MonsterSeed`)와 실체(`Enemy`)를 분리**했다.

```
MonsterSeed (항상 존재)          Enemy (플레이어 근처에서만)
  id, kind, home, position   ◀──▶   컴포넌트, AI, 렌더링
  hpMultiplier, respawnIn
```

- 활성 반경 46 m 안에 들어오면 깨어난다
- 60 m 밖으로 나가면 마지막 위치를 장부에 적고 사라진다
- 동시 활성 상한 **140기**
- 파괴되면 장부에 기록되고 **75초 뒤 원래 자리(`home`)에서 리스폰**

중심에서 멀어질수록 개체 밀도와 상위 기종 비중이 함께 올라간다.
시작 지점 반경 26타일은 비워 둔다.

`lib/game/systems/monster_population.dart`

### 8.4 적은 어디서 오는가

**상주 개체군이 유일한 공급원이다. 웨이브도 스테이지도 판 구분도 없다.**

월드는 하나뿐이고 초기화되지 않는다. "몇 번째 판" 이라 부를 단위가 없으니 난이도를
판 번호로 올리는 축도 존재하지 않는다. 난이도를 정하는 것은 **어디로 가는가** 하나다
— 중심에서 멀수록 상위 기종이 밀집한다(§8.3).

- 파괴된 개체는 제자리에서 75초 뒤 다시 나타난다(`MonsterPopulation.respawnDelay`)
- 그래서 사냥터는 고갈되지 않고, 같은 자리를 계속 도는 것도 유효한 선택이 된다
- 여럿이 한자리에 몰리면 서로 리스폰을 기다리게 된다. 월드를 1 km²로 넓게 잡은 것이
  그 압박을 흩어 놓는다

한때는 상주 개체군과 별개로 플레이어를 추적하는 웨이브 편성이 돌았다. 번호를 매기고
전멸시키면 다음 판으로 넘어가는 구조라 "하나의 영속적인 월드" 와 정면으로 어긋나
걷어냈다. 적이 알아서 찾아오던 편의는 함께 사라졌고, 사냥터를 고르는 일이 플레이어의
몫이 되었다. 다시 넣고 싶어지면 **판을 나누지 않는 형태**여야 한다.

스폰 지점을 찾지 못했을 때의 폴백도 **기준점 둘레로 반경을 넓혀 가며** 찾는다.
월드 전역에서 무작위로 뽑으면 적이 최대 1.4 km 밖에 생겨 어그로 범위(최대 5 m) 밖에
영영 머문다.

### 8.5 피해 수식 — 레벨 항등

**방어력 0인 플레이어가 레벨 N 몬스터에게 맞으면 정확히 N 이 깎인다.**
이것은 곡선이 아니라 항등식이며, 이 게임의 전투 수치에서 가장 굳은 규칙이다.

```
받는 피해 = round( 몬스터레벨 × k/(k+방어력) × 피해감소버프 )     k = 100
```

- 공격력의 단일 출처는 `MonsterSpecies.level` 하나뿐이다(`monster_codex.dart`).
  계통 배율과 개체 편차를 **피해 축에는 일절 걸지 않는다.**
  배율이 하나라도 끼면 화면의 피해 숫자는 반올림되어 그대로 보이므로
  **눈으로는 어긋남을 발견할 수 없다.** `test/combat_damage_test.dart`가 이를 감시한다.
- 개체 편차는 **체력에만** 남아 있다.
- 방어력(`Player.defense`)은 기본 0이고 아직 올릴 방법이 없다. 감산형이 아니라
  승수형인 이유는 몬스터 레벨이 1~200으로 넓어, 감산형이면 방어력이 조금만 올라도
  저레벨 구역이 통째로 무해해지기 때문이다.
- 방화벽(지형 피해)은 무적도 방어력도 무시하며 최대 체력의 4%/틱(0.5초)이다.
  몬스터 레벨과 무관한 별도 축이다.
- 정수화는 **마지막에 한 번만** 한다. HP에서 깎이는 값·화면 숫자·피격음 크기가
  모두 같은 값이어야 하기 때문이다.

체력 축은 이 피해 축과 짝을 이룬다 — 1레벨 2,500, 레벨업마다 +250(레벨 N의 최대 체력 = 2,500 + (N-1) × 250).
큰 체력 풀로 오래 버티는 것이 설계 의도이며, 회복 물약은 자주 들이켜는 소모품이 아니라
가끔 쓰는 한 방(소형 3,750 · 대형 9,000)이다.

> ⚠️ **미해결**: 피격 무적 0.55초가 초당 피격을 1.82회로 묶기 때문에, 안전지대 직외
> (레벨 3 구역)에서는 쉬지 않고 맞아도 이론상 30분을 버틴다. "체력이 다 소진되면 사망"이
> 저레벨 구역에서 사실상 도달 불가능하다는 뜻이다. 해결하려면 무적 시간·출처별 무적·
> 구역 레벨 배치 중 하나를 정해야 한다. 자세한 분석은 `.cowork/hp/final-report.md` §7 권고 9.

### 8.6 정예 개체

같은 구역에서 오래 사냥하면 그 구역 등급의 기종만 반복해 만나므로 모든 교전이
똑같아진다. **정예**(`systems/elite.dart`)는 그 단조로움을 깨는 장치다 — 같은
기종인데 하나가 눈에 띄게 질기거나 빠르고, 그만큼 잡을 값어치가 있다.

| 변종 | 체력 | 속도 | 예비 동작 | 넉백 | 경보 반경 | 색 |
|---|---|---|---|---|---|---|
| 강화 장갑 `fortified` | ×2.6 | ×0.85 | ×1.05 | ×0.35 | 5 m | 파랑 |
| 과부하 `overclocked` | ×1.7 | ×1.45 | **×0.7** | ×1.0 | 5 m | 주황 |
| 경계 지휘 `warden` | ×2.0 | ×1.1 | ×0.9 | ×0.7 | **12 m** | 보라 |

- **피해에는 손대지 않는다.** "레벨 N 로봇은 정확히 N 을 때린다"(§8.5)는 정예도
  예외가 아니다. 배율이 하나라도 끼면 화면의 피해 숫자는 반올림되어 어긋남이
  눈에 띄지 않으므로, `test/elite_test.dart`와 `test/enemy_ai_test.dart`가 이를
  각각 수치와 실전투에서 감시한다. 사거리와 어그로도 그대로다.
- **값어치는 체력을 타고 경험치로 돌아온다.** 체력 배율이 개체 편차와 같은 축에
  실리므로 `LevelSystem.enemyXpValue`가 자동으로 반영한다 — 질긴 만큼 준다.
- **출현율은 깊이에 비례한다.** 시작 지점 `3.5%` → 외곽 `8.5%`. 시작 근처에서도
  드물게는 만나야 "정예라는 것이 있다"를 배우고, 깊이 들어갈 이유가 된다.
- **장부가 들고 있다.** `MonsterSeed.elite`이므로 멀어져 실체가 사라졌다 다시
  깨어나도 같은 자리의 같은 개체는 계속 정예다. 지나친 그 정예를 다시 찾아갈 수
  있어야 한다.
- **한눈에 다르게 보인다.** 발밑에 맥동하는 두 겹 고리와 네 방향 눈금(등급
  문양의 겹고리와는 다른 모양), 이름표 앞에 변종 이름이 고리와 같은 색으로 붙고,
  거리와 무관하게 이름표를 항상 단다.

---

## 9. 아이템과 성장

### 9.1 전리품

**회복 물약은 다섯 등급뿐이다.**

| 등급 | 종류 | 회복 | 잡몹 드롭 | 비고 |
|---|---|---|---|---|
| 1 | `nanoVial` REPAIR VIAL | HP 100 | 16~30% | 유일하게 흔하다 |
| 2 | `nanoCanister` NANO CANISTER | HP 200 | 1.2~3.5% | 장갑 경화 버프가 딸린다 |
| 3 | `repairCell` REPAIR CELL | HP 300 | 0.8~1.5% | |
| 4 | `regenAmpoule` REGEN AMPOULE | HP 500 | 0.4% | 중장갑 이상에서만 |
| 5 | `overhaulKit` OVERHAUL KIT | HP 1000 | 0.1% | 사실상 구역 보스 전용 |

1레벨 최대 체력이 2,500이므로 **가장 좋은 물약도 한 병에 40%**다. 이것은 의도된
설계다 — 회복은 물약에 기대는 것이 아니라 **큰 체력 풀로 오래 버티는 것**이며,
물약은 사냥 지속 시간을 좌우하는 자원이 아니라 곁다리다. 그래서 2등급 이상은
드롭 확률을 아주 낮게 눌러 두었다. `test/combat_damage_test.dart`가 이 정책을 감시한다.

| 그 밖의 전리품 | 효과 | 등급 |
|---|---|---|
| `energyCell` 에너지 셀 | EN 32 | common |
| `overchargeCell` 과충전 셀 | EN 75 | rare |
| `combatStim` 전투 강화제 | 버프(회복은 곁다리 25) | rare |
| `dataChip` 데이터 칩 | XP 18 | uncommon |
| `scrapCore` 스크랩 코어 | 점수 40 | uncommon |
| `weaponCache` 무기 상자 | 무기 한 자루 (§9.4) | rare |

무기 상자만은 종류 하나로 정해지지 않는다 — 이름도 색도 **안에 든 무기**에서
오므로, 바닥에 떨어진 상자의 발광색이 곧 그 무기의 등급색이다. 멀리서 색만 보고
주우러 갈지 정할 수 있어야 하기 때문이다.

전리품은 잔해에서 **포물선을 그리며 튀어나와** 최대 2번 튕기고 착지한다
(중력 11, 반발 계수 0.42). 0.18초 후 자석이 작동해 플레이어 쪽으로
가속하며 빨려 들어간다. 수명 26초, 마지막 4초는 깜빡인다.

드롭은 기종별 `DropTable`이 항목마다 독립적으로 굴린다 (`systems/drop_table.dart`).

### 9.2 인벤토리와 버프

포션은 즉시 쓰이지 않고 **가방에 보관**된다. 칩·코어처럼 즉시 환산되는 전리품만
바로 소비된다. 가방이 가득 차면 포션은 끌려오지도, 회수되지도 않고 바닥에 남는다.

포션을 마시면 일정 시간 버프가 걸린다 (`systems/buff.dart`).
같은 종류를 다시 마시면 지속 시간이 갱신되고, **다른 종류의 배율은 서로 곱해진다**.
버프는 피해량·이동 속도·받는 피해·에너지 회복에 작용한다.

### 9.3 레벨

`systems/level_system.dart` — 성장은 전부 이 클래스를 거치므로 밸런스 조정은
여기만 고치면 된다.

```
필요 경험치 = 60 × 1.22^(level-1)     만렙 30
```

| 레벨업 보상 | 일반 | 5의 배수 (강화 구간) |
|---|---|---|
| 최대 HP | +18 | +34 |
| 최대 에너지 | +8 | +14 |
| 근접 피해 | +4.5 | +8.0 |
| 원거리 피해 | +3.0 | +5.5 |
| 이동 속도 | — | +0.08 |

레벨 차이에 따른 경험치 감소에는 **하한 0.55**가 있다. 이 하한이 없으면 더 깊은
구역으로 나가 얻는 보상 증가분보다 감소분이 커져 20레벨 부근에서 성장이 사실상 멈춘다.

그렇게 깎인 값에 마지막으로 `killXpMultiplier`(현재 **×3.0**)를 곱한 것이 처치
보상이다. 곡선은 서버 `leaderboard.rs`와 상수를 맞춰 둔 정본이라 손대면 양쪽
레벨 계산이 갈리므로, 사냥 속도는 이 배율 하나로만 조절한다. 배율은 클라이언트가
지급 시점에 곱할 뿐이고 서버로는 이미 곱해진 누적 경험치만 올라간다.

### 9.4 무기

`systems/weapon.dart` — 무기는 **근접·원거리 피해에 곱해지는 배율 하나와
등급(겉모습·사거리)** 로 이루어진다. 손에 드는 무기가 오는 길은 둘이다.

**① 기본 무기 — 레벨의 함수.** `WeaponSystem.forLevel(level)`. 레벨업 한 번이
곧 무기 강화 한 번이고, 정해진 경계 레벨(1·5·10·20·35·55·80·120·180·280·450·700)
에서 등급이 바뀌며 이름·색·날의 수·길이가 함께 바뀐다. 그 위에 **5레벨마다
계통이 갈린다**(§9.6). 저장하지 않고 레벨에서 유도하므로 — 등급도 계통도 —
서버가 주고받는 것은 여전히 누적 경험치뿐이고, 같은 월드의 모든 클라이언트가
남의 무기까지 똑같이 그린다.

위력 곡선은 포화형이다 — `1 + 1.2 × (1 − 0.99^(level−1))`. 근접·원거리 피해는
레벨마다 이미 **더해서** 자라는데 무기까지 선형이면 둘의 곱이 제곱으로 뻗기
때문이다. 만렙에서도 무기가 기여하는 몫은 ×2.2 에 머문다.

**② 주운 무기 — 로봇 잔해에서 굴린다.** 파괴된 로봇은 확률적으로 무기 상자
(`PickupKind.weaponCache`)를 떨구고, 그 안의 무기는 **떨군 로봇의 레벨 ±3** 과
**벼림 등급**, 그리고 **계통**을 굴려 만든다. 강한 구역일수록 좋은 무기가
나오므로, 어디서 사냥할지가 곧 무기를 고르는 일이 된다.

| 벼림 | 위력 배율 | 무게 |
|---|---|---|
| `worn` WORN | ×0.90 | 34 |
| `field` (이름 없음) | ×1.00 | 34 |
| `honed` HONED | ×1.08 | 20 |
| `prime` PRIME | ×1.16 | 9 |
| `apex` APEX | ×1.25 | 3 |

| 떨구는 쪽 | 무기 확률 |
|---|---|
| `scout` 정찰 드론 | 1% |
| `sentry` 순찰 보행 로봇 | 2% |
| `heavy` 중장갑 메크 | 4.5% |
| `commander` 지휘 유닛(보스) | 35% |
| `crate` 보급 상자 | 0.8% |

무기 상자는 드롭 표의 다른 항목보다 **먼저** 굴려 목록 앞에 둔다. 뒤에 붙이면
가장 드문 전리품이 하필 `maxDrops` 에 잘려 나간다. 웨이브 행운 보정도 더하지
않는다 — 소모품 확률과 자릿수가 달라 같은 값을 더하면 후반에 무기가 소모품보다
흔해진다.

### 9.5 무기 계통 — 일곱 가지 싸우는 방식

등급이 "얼마나 좋은 무기인가"라면 **계통(`WeaponClass`)은 "어떤 무기인가"다.**
등급은 레벨을 따라 오르는 값이라 순서가 있지만, 계통은 서로 우열이 없는
**맞바꿈**이다. 계통은 두 길로 손에 들어온다 — 5레벨마다 저절로(§9.6),
그리고 잔해에서 주워서.

| 계통 | 색 | 피해 | 속도 | 사거리 | 부채꼴 | 콤보 | 드롭 무게 |
|---|---|---|---|---|---|---|---|
| `blade` 블레이드 | 등급 색 그대로 (청록→금빛) | ×1.00 | ×1.00 | +0 | 120° | 3타 | 30 |
| `maul` 파일 해머 | 호박색 (hue 55) | ×1.35 | ×0.68 | +0.35 | 150° | 2타 | 14 |
| `lance` 이온 랜스 | 연두 (hue 100) | ×0.78 | ×1.30 | +0.75 | 55° | 3타 | 15 |
| `reaper` 리퍼 | 보라 (hue 281) | ×0.95 | ×0.95 | +0.20 | 320° | 2타 | 12 |
| `driver` 파일 드라이버 | 진홍 (hue 10) | ×1.55 | ×0.48 | +0.10 | 45° | 1타 | 8 |
| `talon` 트윈 탈론 | 에메랄드 (hue 145) | ×0.72 | ×1.44 | +0.05 | 95° | 4타 | 11 |
| `vortex` 볼텍스 디스크 | 청색 (hue 236) | ×0.92 | ×1.08 | +0.55 | 190° | 3타 | 10 |

뒤의 셋이 하는 일은 앞의 넷이 비워 둔 자리를 메우는 것이다. **드라이버**는
콤보가 없는 유일한 계통이라 매 타가 마무리(×1.6)다 — 다른 계통이 세 번에
나눠 하는 일을 한 번에 하고, 빗나가면 그 한 번을 통째로 잃는다. **탈론**은
반대쪽 끝이다: 가장 빠르고 가장 길게(4타) 이어지되 사거리가 블레이드보다도
짧아 맞으면서 싸우게 된다. **볼텍스**는 넓이와 사거리를 함께 가진 유일한
계통이고, 그 대가로 세기·속도가 모두 어중간하다.

**색이 계통을 가르는 첫 번째 단서다.** 실루엣은 붙어야 보이지만 색은 화면
반대편에서도 보인다 — 바닥의 상자를 주우러 갈지, 저쪽에서 오는 요원이 무엇을
들었는지가 색 하나로 정해진다. 그래서 계통은 **색상(hue)** 을, 등급은 **밝기**를
맡는다(`WeaponClass.tint`). 등급 색에서 색상만 갈아 끼우고 밝기는 그대로 두므로
두 정보가 한 색 안에 겹쳐 들어간다.

블레이드만 등급 색을 그대로 쓴다. 레벨을 따라 청록에서 금빛으로 흐르는 그 색이
**레벨업이 무기를 바꾼다**는 성장의 신호이기 때문이다. 대신 높은 등급의 블레이드는
보라·금빛으로 흘러 리퍼·해머와 색이 겹칠 수 있다. 겹치는 자리는 실루엣과 이름이
갈라 준다.

나머지 여섯의 색상각은 **1레벨 블레이드(190°)에서 45°씩 벌린 격자** 위에 놓여
있다 — 10·55·100·145·190·236·281. 자리가 정확히 다 찼다: 마젠타 대역(적의 색,
300~355)을 뺀 305°에 일곱을 60°씩 벌려 놓으려면 420°가 필요하다. 45°는 채도
높은 색끼리는 확실히 갈리는 폭이고, **여기서 계통을 더 늘리려면 색이 아니라
실루엣에 기대야 한다.** 그래서 뒤에 더한 셋은 실루엣부터 서로 겹치지 않게
잡았다 — 짧고 굵은 통(드라이버), 세 갈래 갈고리(탈론), 자루가 없는 링(볼텍스).

채도의 바닥(0.6)과 밝기의 천장(0.82)을 함께 두는 이유는 최상위 등급 색이 흰빛으로
수렴하기 때문이다. 흰색은 채도가 0이라 색상각을 돌려도 흰색이고, 밝기가 1이면
채도를 올려도 여전히 흰색이다 — 둘 중 하나만 막으면 만렙 무기가 계통과 무관하게
전부 하얘진다.

**일곱 계통의 초당 피해는 서로 5% 안쪽이다** (`피해 × 속도 × 콤보 평균`, 콤보
마지막 타는 ×1.6). 한 계통이 수치로 앞서면 나머지는 손에 들어왔을 때 실망하는
무기가 되고, 계통이 있다는 사실 자체가 무의미해진다. 고르는 근거는 세기가 아니라
**부채꼴의 넓이와 사거리**여야 한다. 어느 계통도 네 축(피해·속도·사거리·부채꼴)
모두에서 다른 계통을 앞서지 않는다 — `test/weapon_drop_test.dart` 가 둘 다 감시한다.

이 균형이 전보다 중요해졌다. 주워야만 만나던 시절에는 실망스러운 계통을 안
주우면 그만이었지만, **5레벨마다 저절로 물리는 지금은 안 고를 수가 없다.**

그래서 **바꿔 들지는 위력이 아니라 `Weapon.dps` 로 정한다.** 위력만 보면 느리고
한 대가 큰 망치가 언제나 이겨 계통이 맞바꿈이 아니라 서열이 된다.

**계통마다 궤적이 통째로 다르다** (`entities/weapon_art.dart`).

| 계통 | 애니메이션 | 표면 무늬(절차적 텍스처) |
|---|---|---|
| 블레이드 | 부채꼴 베기. 콤보마다 방향이 뒤집히고 마무리는 1.6π를 훑는다 | 칼날에 새긴 가로 눈금 — 등급이 오를수록 촘촘 |
| 파일 해머 | 들어 올리기(35%) → 내리찍기(35%) → 착탄 후 정지(30%). 머리가 지면에 닿는 진행도 0.70에서 충격파 고리가 퍼진다 | 판금 머리에 배기 슬릿과 리벳, 앞면에 심지색 타격면 |
| 이온 랜스 | 찌르기. 앞 35%에 빠르게 뻗고 나머지에 천천히 거둔다 | 창대의 마디 고리 + 좁고 긴 마름모 창끝 |
| 리퍼 | 몸을 축으로 한 바퀴. 지나온 만큼 고리가 남는다 | 속이 빈 초승달 날의 등에 톱니 |
| 파일 드라이버 | 장전(45%) → 격발(27%) → 반동. 각도가 아니라 **뻗는 거리** 하나로 이야기한다. 착탄에서 통이 뒤로 밀리고 옆으로 배기가 뿜는다 | 약실의 압력 눈금 + 총구 링, 격발 순간의 총구 화염 |
| 트윈 탈론 | 짧게 긁기. 콤보마다 방향과 높이를 바꿔 네 타가 서로 다른 자리를 긋는다 | 나란한 세 줄의 자국 + 안쪽으로 휜 갈고리 세 갈래 |
| 볼텍스 디스크 | 던졌다 되받기. 각도는 한 방향으로 흐르고 거리는 `sin(πt)` 로 나갔다 돌아온다 — 가장 멀리 나간 순간이 정확히 정면 | 톱니 다섯의 빈 고리 + 손과 원반을 잇는 자기력선 |

드라이버가 `finisher` 를 받지 않는 것은 콤보 길이가 1이라 **모든 타가 마무리**
이기 때문이다. 마무리일 때만 커지는 연출은 거기서 "언제나 커진다"와 같은 말이다.

볼텍스의 자기력선과 잔상은 장식이 아니다. 그것이 없으면 원반이 날아가 사라지는
그림이 되어 **돌아온다**는 사실이 화면 어디에도 없다.

**타격 판정 시점은 그림과 같은 값을 읽는다** (`WeaponClass.hitAt`). 무게로
때리는 계통만 늦다 — 해머 0.70, 드라이버 0.72. 탈론은 가장 빨라 0.30이고 나머지는
0.35다. 둘이 어긋나면 맞은 순간이 화면에 없는 공격이 된다. 같은 이유로 값을
`WeaponClass` 한 곳에 두어 그림과 판정이 **같은 상수를 읽게** 했다.

계통은 이름·바닥 아이콘·HUD 글리프·등에 멘 실루엣에 모두 드러난다. 무엇을 든
사람인지 붙기 전에 알 수 있어야 PK 판단이 성립하기 때문이다. 그림이 실제로
나오는지는 `test/weapon_art_render_test.dart` 가 일곱 계통 × 스윙 전 구간을
그려 보며 지킨다 — 특정 진행도에서만 터지는 값(음수 반지름, 시그마 0의 블러)은
화면에 띄우기 전에는 드러나지 않는다.

**손에 드는 것은 둘 중 센 쪽이다.** 어느 하나를 "장착 상태"로 따로 들고 있지
않는 것이 요점이다. 그러면 레벨이 자라 기본 무기가 주운 것을 앞질렀는데도 옛
무기를 든 상태가 생기고, 그 어긋남을 지우려면 레벨업마다 장착을 손봐야 한다.
매번 견주면 그런 상태 자체가 없다. 판단 기준은 위력 하나다 — 등급은 겉모습이고
사거리는 등급에서 따라오므로 위력으로 고르면 셋이 함께 따라온다.

자동 사냥이 바닥의 전리품을 모조리 빨아들이므로 **집는 대로 갈아 끼우지 않는
것**도 같은 이유다. 그랬다면 잡몹이 떨군 WORN 한 자루에 무기가 도로 약해진다.
못한 무기는 회수는 되되 `SALVAGED` 라벨만 뜨고 손에 들리지 않는다.

**아직 남은 것: 주운 무기는 남에게 보이지 않는다.** 서버가 아는 것은 누적
경험치뿐이므로(§12.4) 다른 사람의 화면에는 그 사람의 **레벨 무기**가 그려진다
(`RemotePlayer` 가 `WeaponSystem.forLevel(level)` 을 등에 메워 준다). 등급도
계통도 레벨의 함수라 그 그림은 정확하다 — 저쪽 요원이 든 것이 해머인지 링인지가
레벨 하나로 갈린다. 주운 무기까지 보이게 하려면 `world_player` 에 무기를 실어야
하는데, 레벨과 벼림은 정수 하나로 접히므로 열 하나면 된다. 접속 때 주운 무기를
잃는 것도 같은 곳에서 풀린다(`Player.restoreProgress` 가 지금은 버린다).

### 9.6 5레벨마다 갈리는 계통

**레벨업 다섯 번마다 팔에 다른 무기가 물린다.** `WeaponSystem.classForLevel` —
`innateCycle[(level ~/ 5) % 7]` 한 줄이 전부다.

| 레벨 | 1~4 | 5~9 | 10~14 | 15~19 | 20~24 | 25~29 | 30~34 | 35~ |
|---|---|---|---|---|---|---|---|---|
| 계통 | 블레이드 | 해머 | 랜스 | 드라이버 | 탈론 | 리퍼 | 볼텍스 | 처음으로 |

설계에서 정한 것이 넷 있다.

**① 끝나지 않고 돈다.** 목록이 소진되면 멈추는 것이 아니라 처음으로 돌아온다.
그래야 만렙까지 "5레벨마다 새 무기"가 계속 참이다. 돌아왔을 때는 등급이 올라
있어 이름·색·날의 수가 다르므로 "또 그 칼"이 아니라 "그 칼의 다음 판"이 된다.
일곱 계통이 한 바퀴에 35레벨, 만렙까지 스물여덟 바퀴다.

**② 주기를 스탯 강화 구간(`level % 5 == 0`)에 정확히 얹었다.** 계통끼리 초당
피해가 최대 5% 다르므로, 계통만 갈리는 레벨이 있으면 그 레벨에서 새 무기가
**약해진 것**으로 읽힐 수 있다. 스탯이 가장 크게 뛰는 레벨에 겹쳐 두면 그
차이가 도약에 묻힌다. 두 주기가 어긋나면 안 되는 이유이고, 테스트가 지킨다.

**③ 차례는 이웃끼리 최대한 다르게.** 느린 것 다음에 빠른 것, 좁은 것 다음에
넓은 것 — 5레벨을 들여 받은 무기가 방금 쓰던 것과 리듬까지 비슷하면 바뀐 줄도
모른다. 첫 칸이 블레이드인 것은 그것이 사이보그의 팔에 처음부터 달려 있는
계통이라서다. 시작하자마자 낯선 무기를 쥐여 주지 않는다.

**④ 여전히 레벨 하나의 순수 함수다.** 굴림이 끼지 않으므로 서버가 주고받는
것은 누적 경험치뿐이라는 전제(§12.4)가 그대로 유지되고, 남의 화면과 내 화면이
같은 무기를 그린다. 계통을 저장했다면 동기화해야 하는 상태가 하나 늘었을 것이다.

바뀐 순간은 배너 `▲ WEAPON <이름>` 과 그 무기 색의 불꽃으로 알린다. 이름은
`Weapon.title`(등급 앞말 + 계통 끝말)이라 벼린 횟수 `+7` 에 정작 바뀐 것이
묻히지 않는다. **손에 든 것이 주운 무기라면 배너는 뜨지 않는다** — 기본 무기가
무엇으로 갈리든 화면의 무기는 그대로이므로, 그때 알리면 바뀌지 않은 것을 알리는
셈이 된다.

---

## 10. 오디오

`lib/game/audio/game_audio.dart` — flame_audio 기반.

- 효과음 27종 (배리에이션 포함 40여 파일)
- 같은 소리를 무작위 배리에이션으로 재생해 반복감을 줄인다
- **최소 재생 간격**을 두어 짧은 시간에 몰릴 때 소리가 찢어지는 것을 막는다
- 위치 기반 감쇠 — `at:` 인자로 그리드 좌표를 넘기면 거리에 따라 볼륨이 준다
- 첫 타격에서 소리가 밀리지 않도록 `onLoad`에서 미리 로드한다
- **볼륨은 기기에 남는다**(`audio/audio_settings.dart`). 배경음·효과음·음소거를
  `shared_preferences`에 저장하고 `GameAudio.init` 다음에 되돌려, 첫 소리부터 고른
  크기로 난다. 계정이 아니라 기기에 붙는 값이라 서버에 두지 않는다 — 스피커가 큰
  데스크톱에서 줄인 값이 휴대폰까지 따라가면 오히려 성가시다.
- 저장소를 열 수 없거나 오디오 장치가 없으면 조용히 기본값·무음으로 남는다.
  소리 때문에 게임이 멈추는 일은 없다.

---

## 11. UI / UX

### 11.1 진입 흐름

```
CyborgGate  ─▶ 연결 중 ─▶ 로그인 ─▶ 캐릭터 선택 ─▶ [출격] ─▶ GameScreen
```

어떤 화면을 보여줄지는 **전부 서버 상태에서 도출한다**. 각 화면이 직접 전환하지
않고 관문 한 곳에 모아 두었기 때문에, 다른 기기에서 로그아웃하거나 세션이 끊겨도
화면이 저절로 제자리를 찾는다.

캐릭터를 골랐다고 바로 들어가지 않는다 — 사용자가 "출격"을 눌러야 한다.

### 11.2 HUD

- 좌상단: 레벨 배지, HP / 에너지 / XP 게이지, 대시 쿨다운, 처치 수, 점수
- 상단 중앙: 현재 구역 이름, 월드 접속자 수 (안전지대에서는 테두리가 밝아진다)
- 우상단: 미니맵
- 하단 중앙: 포션 퀵슬롯
- 좌하단/우하단: 가상 조이스틱, 액션 버튼 3개 (모바일)
- 좌하단(조이스틱 위): 확대·축소 버튼과 현재 배율
- 오버레이: 피격 비네트, 저체력 맥동, 콤보/배너 텍스트

**시야 배율은 두 몫의 곱이다** (`camera_zoom.dart`). 화면 높이가 정하는 기본
배율(760px 기준, 0.55~1.6로 제한)에 플레이어가 버튼으로 고른 배율
(50 / 65 / 80 / 100 / 125 / 160 / 200%)을 곱한다. 하나로 합쳐 두면 창 크기가
바뀔 때마다 고른 배율이 지워지고, 곱셈으로 겹치면 어느 창에서도 선택이 남는다.
단계를 미리 못 박은 이유는 버튼이기 때문이다 — 비율을 곱해 나가면 상·하한에서
잘려 되돌려도 원래 보던 배율로 정확히 돌아오지 못한다.

하한을 50%로 잡은 것은 스트리밍 때문이다. 가장 넓게 물러난 배율에서도 화면
모서리가 닿는 거리는 큰 모니터에서 30 m 남짓으로, 로봇을 깨우는 반경(46 m)
안에 머문다. 구조물은 시야에서 직접 계산하므로(`visibleGridBounds`) 저절로
따라오지만, 로봇은 플레이어까지의 거리로만 깨어나기 때문에 이 하한을 더 내리면
화면 가장자리에 텅 빈 땅이 드러난다.

### 11.3 패널

캐릭터 화면(`C`), 리더보드(`B`), 인벤토리(`I`/`Tab`), 월드 지도(`M`),
소리 설정(`O`), 텔레포트, 월드 메뉴(`Esc`).

**월드 지도**(`ui/world_map_screen.dart`)는 1 km² 월드를 한 장에 담는다. 지형은
열 때 한 번 256×256 축소판으로 구워 두고(칸 100만 개를 매 프레임 훑을 수는 없다),
그 위에 **위험 등급 고리**(Lv.10·25·50·100·200)를 얹는다. 고리의 반지름은
`MonsterCodex.regionDistanceFor` — §8.1 배치 곡선의 역함수라, 지도가 말하는
등급과 실제 주둔 병력이 어긋날 수 없다. 안전지대·텔레포트 목적지·다른 요원도
함께 찍히고, 지도를 누르면 그 지점의 위험 등급과 거리를 재 준다.

여태 화면에는 반경 30 m 근접 레이더뿐이어서, 레벨 12 요원이 무심코 3분을 걸어
레벨 80 구역에 들어가도 알 길이 없었다. 같은 이유로 HUD 레이더 아래에도 **현재
구역 위험도**를 지도와 같은 색으로 상시 표시한다.

**패널이 열려 있어도 게임은 멈추지 않는다.** 월드는 모두가 공유하는 실시간
공간이므로 한 플레이어의 UI 조작이 시간을 멈출 수 없다. 이는 MMO 설계에서
따라오는 필연적 제약이다.

### 11.4 조작

| 입력 | 동작 |
|---|---|
| `WASD` / 방향키 | 이동 (화면 기준 → 그리드 방향으로 자동 변환) |
| `Space` / `J` | 근접 공격 |
| `K` / `F` | 플라즈마 발사 |
| `Shift` / `L` | 대시 |
| `Q` | 회복 포션 자동 사용 |
| `1`~`6` | 퀵슬롯 포션 |
| `C` / `B` / `I`·`Tab` | 캐릭터 / 리더보드 / 인벤토리 |
| `M` | 월드 지도 |
| `O` / `V` | 소리 설정 / 음소거 |
| `Esc` | 열린 창 닫기 → 없으면 일시정지 |
| `P` | 일시정지 |

키보드 입력은 화면 기준 방향으로 받아 `screenDirToGridDir()`로 변환한다.
`W`는 화면 위쪽이지 그리드 `-y`가 아니다 — 아이소메트릭에서 이 변환을 빼먹으면
조작이 45도 틀어진다.

---

## 12. 백엔드 (SpacetimeDB)

### 12.1 배포 정보

| 항목 | 값 |
|---|---|
| 데이터베이스 | `withcenter-cyborg` |
| 서버 | `maincloud.spacetimedb.com` |
| 모듈 경로 | `./spacetimedb` (Rust → wasm32) |
| 배포 | `spacetime publish withcenter-cyborg --yes` |

### 12.2 스키마

| 테이블 | 공개 | 내용 |
|---|---|---|
| `account` | 비공개 | 계정. 이메일(정규화·unique), 생성/최종 로그인 시각 |
| `account_secret` | 비공개 | 비밀번호 해시 + salt. **어떤 view도 이 표를 읽지 않는다** |
| `session` | 비공개 | identity ↔ 계정 연결. 선택된 캐릭터 |
| `player_character` | 비공개 | 캐릭터. 이름, 종류, 레벨, 경험치 |
| `leaderboard_entry` | 비공개 | 행을 한 줄도 넣지 않는 표 (아래 설명) |
| `world_player` | **공개** | 월드에 들어와 있는 캐릭터. 위치·체력·공격 쿨다운 |
| `monster` | **공개** | 서버 권위 몬스터 240기. 선점자(`tagged_by`) 포함 |
| `monster_kill` | **공개** | 서버가 확정한 킬 기록 |
| `monster_tick_timer` | 비공개 | 리스폰 스케줄 타이머 |

**계정 계층은 전부 비공개, 월드 계층만 공개다.** 비공개 원칙이 지키려는 것은
남에게 갈 이유가 없는 값(비밀번호 해시, `account_id`)이지 월드에 드러난 사실이
아니다. 다른 요원이 어디 서 있고 어떤 몹이 살아 있는지는 게임이 성립하려면
서로 보여야 한다. 대신 공개 표에는 계정을 가리키는 열을 하나도 두지 않는다.
본인 계정 데이터는 여전히 view로만 읽는다.

| View | 반환 |
|---|---|
| `my_account` | 내 계정 |
| `my_session` | 내 세션 |
| `my_characters` | 내 캐릭터 목록 |
| `leaderboard` | 상위 100위 (레벨 ↓ → 경험치 ↓ → 생성순) |
| `my_rank` | 100위 밖일 때의 내 순위 |

### 12.3 보안 설계

**비밀번호를 다른 표에 둔 이유.** `AccountSecret`을 `Account`에서 떼어 놓았으므로
계정 정보를 노출하는 **어떤 경로도** 해시를 함께 흘릴 수 없다. "실수로 같이
나가는" 일이 구조적으로 불가능하다.

**해시.** SHA-256을 30,000회 늘린다. wasm에서 수십 밀리초 수준이라 reducer 실행
시간 안에 들어오고, 공격자에게는 시도당 같은 비용을 강제한다.
비교는 **입력에 따라 시간이 달라지지 않게** 한다 (타이밍 공격 차단).

**비밀번호 규칙은 길이만 본다** (8~128자). 대문자·기호 강제는 실제 강도보다
예측 가능한 변형(`Password1!`)을 유도하는 효과가 크다는 판단이다.

**캐릭터 종류는 서버 화이트리스트로 검증한다.** reducer는 누구나 임의 인자로
부를 수 있으므로 클라이언트 UI가 두 가지만 보여준다는 사실을 믿지 않는다.

**리더보드 표에 행을 넣지 않는 이유.** 순위는 view가 그때그때 계산하고 어디에도
저장하지 않는다. 그런데도 표로 선언한 것은 클라이언트 코드 생성기가 view 반환
타입의 이름을 표 목록에서만 찾기 때문이다 — 표가 아니면 존재하지 않는 클래스를
참조하는 코드를 뱉는다.

### 12.4 동기화 계층

`GameSync`(인터페이스) ← `SpacetimeGameSync`(구현).

**올리는 것은 레벨과 경험치뿐이다.** 점수·처치 수는 리더보드가 쓰지 않으므로
보내지 않는다. 쓰지 않는 값을 보내면 서버는 저장할 곳을 마련해야 하고, 그때부터
아무도 보지 않는 열이 늘어난다.

- 주기 전송 5초 (레벨업은 즉시 전송)
- 같은 값은 두 번 보내지 않는다
- 서버가 거절하면(로그인 전·캐릭터 미선택) 재시도를 멈춘다 — 로그만 더럽히므로

### 12.5 서버 권위 월드와 킬 판정 (`world.rs`)

몬스터와 킬 판정을 서버로 올린 계층이다. **배포는 되어 있으나 클라이언트는 아직
이 reducer들을 부르지 않는다** — 연결은 다음 과제다(13장).

#### 왜 몬스터가 서버에 있어야 하는가

하나의 오픈월드를 여럿이 공유하므로 몬스터는 각자의 화면에 따로 존재하는 연출이
아니라 **모두가 같은 것을 보는 하나의 개체**여야 한다. 클라이언트가 자기 몫을
따로 굴리면 A가 쓰러뜨린 몹이 B 화면에는 살아 있고, 그 순간 "하나의 월드"라는
전제가 깨진다. 파티 협업이 없어도 이 요구는 그대로다 — 이유가 협동이 아니라
**단일 월드**이기 때문이다.

#### 킬 판정은 선점(태그) 방식이다

몹을 **처음 때린 사람**이 주인이 되고, 누가 막타를 넣든 경험치는 주인에게 간다.
막타 방식을 쓰지 않은 이유는 남이 깎아 둔 몹을 가로채는 짓에 보상을 주기 때문이다.

이 규칙은 PK와 맞물려 돌아간다. 남이 선점한 몹을 뺏고 싶으면 몹이 아니라
**그 사람**을 쓰러뜨려야 한다. 사냥터 다툼이 그대로 PK의 동기가 된다.

주인이 자리를 떠도 몹이 영영 묶이지 않도록 태그에는 수명(30초)이 있다. 태그
갱신은 **주인이 때렸을 때만** 한다 — 남이 때릴 때도 갱신하면 가로채려는 쪽이
태그를 붙잡아 둘 수 있다.

막타를 넣은 사람이 주인과 다르면 `monster_kill.last_hit_by`에 기록만 남긴다.
보상에는 영향이 없고, 분쟁이 어디서 일어나는지 나중에 들여다보기 위한 흔적이다.

#### 같은 몹에 막타가 동시에 들어오면

reducer는 원자적 트랜잭션이고 격리 수준이 strongly serializable이다. 같은 몹을
향한 두 호출은 직렬화되어, 먼저 커밋된 쪽이 `alive = false`로 만들고 나중 호출은
이미 죽은 몹을 보고 그대로 실패한다. **둘 다 킬을 먹거나 둘 다 놓치는 상태가
원리적으로 생기지 않는다.** 몹마다 락을 걸거나 큐를 두지 않는 이유가 이것이다.

단 SpacetimeDB는 직렬화 이상을 감지하면 같은 인자로 reducer를 **재실행할 수
있다.** 그래서 판정에 쓰는 상태는 전부 표 안에만 둔다. 모듈 전역 변수에 무언가를
세어 두면 재실행될 때 두 번 세어진다.

#### reducer

| reducer | 하는 일 |
|---|---|
| `enter_world` | 고른 캐릭터로 입장. 자리는 **항상 안전지대 한가운데** |
| `leave_world` | 퇴장 |
| `move_to(x, y)` | 좌표 보고. 서버는 속도 상한만 검증 |
| `attack_monster(id)` | **핵심.** 쿨다운 → 사거리 → 선점 → 피해 → 킬 확정 |
| `monster_tick` | 5초마다 쓰러진 몹을 리스폰 |
| `ensure_world_populated` | 빈 월드에 몹을 채운다(멱등) |

클라이언트는 **"몇 번 몹을 친다"는 의도만** 보낸다. 피해량·사거리·쿨다운은 전부
서버가 자기 값으로 정한다. 클라이언트가 피해량을 보내는 구조였다면 판정 전체가
무의미해진다.

입장 자리를 마지막 위치가 아니라 안전지대로 고정한 것은, 사냥터 한복판에서
로그아웃해 위험을 회피하는 짓을 막기 위해서다. 접속이 끊기면 `world_player`
행을 즉시 지운다 — 남겨 두면 조종하는 사람 없는 캐릭터가 몹을 선점만 하고 서 있게 된다.

#### 이동은 절충안이다

클라이언트가 좌표를 보내고 서버는 **속도 상한만** 본다(14 타일/초). 완전한 서버
이동 시뮬레이션은 조작에 더 강하지만 예측·롤백을 함께 만들어야 하고, 그 비용이
지금 얻는 것보다 크다. 여기서 막으려는 것은 "월드 반대편으로 순간이동해 기습하고
사라지는" 짓이지 미세한 속도 조작이 아니다.

상한을 넘으면 거절하는 대신 **갈 수 있는 데까지만 당겨서** 받아들인다. 거절하면
지연이 한 번 튈 때마다 화면이 뒤로 끌려가 조작감이 무너진다.

#### 경험치 곡선은 정본이 하나다

`world.rs`는 `leaderboard.rs`의 `XP_TO_NEXT` 표를 그대로 쓴다. 같은 곡선을 따로
계산하면 한쪽만 고쳤을 때 조용히 어긋나고, 그때 나타나는 증상은 "레벨업이
리더보드 검증에 막혀 되돌아가는 것"이라 원인을 찾기 어렵다.

---

## 13. 목표 대비 현재 위치

`CLAUDE.md`가 선언한 MMORPG 요구사항 기준.

| 요구사항 | 상태 |
|---|---|
| 단일 공유 오픈월드 (인스턴스 없음) | ✅ 1 km² 월드, 시드 기반 결정적 생성 |
| 계정 · 캐릭터 · 영속 성장 | ✅ 서버 저장, 리더보드 반영 |
| 파티 (동행·리더 추종) | ⚠️ 멤버십·초대는 서버 `party` 모듈, 추종은 클라이언트 판단. 보상은 나누지 않는다 |
| 솔로 보상 (파티여도 비공유) | ✅ 선점 태그 기반 단독 크레딧. 파티는 위치 공유와 추종 앵커만 제공한다 |
| 다수 플레이어 동시 접속 | ⚠️ 계정·리더보드 연결됨. 서버에 `world_player` 표 생김. **클라이언트가 아직 안 씀** |
| 다른 플레이어 실시간 표시 | ⚠️ 서버 표는 준비됨. 렌더링 미구현 |
| 몬스터가 공유 객체 | ⚠️ **서버 권위 몬스터 240기 배포 완료.** 클라이언트는 아직 로컬 개체군을 씀 |
| 처치 귀속 (누가 잡았는가) | ✅ **선점(태그) 방식으로 서버가 판정** (12.5절) |
| PK (PC 간 전투) | ❌ 미구현. `world_player`에 체력·좌표가 있어 얹을 준비는 됨 |

### 남은 핵심 과제

서버 쪽 관문은 넘었다(12.5절). 남은 것은 **클라이언트를 그 서버에 붙이는 일**이다.

1. **바인딩 재생성** — `world_player` · `monster` · `monster_kill` 표가 늘었으므로
   `lib/spacetime/generated/`를 다시 만들어야 한다.
2. **입장 흐름 연결** — 출격 시 `enter_world`를 부르고, 이동은 `move_to`로 좌표를
   올린다. 지금은 게임이 서버에 아무것도 보고하지 않는다.
3. **몬스터를 서버 표로 교체** — 현재 `MonsterPopulation`은 클라이언트가 시드로
   생성한 로컬 장부다. 이것을 `monster` 표 구독으로 바꾸고, 공격은
   `attack_monster`를 부른 뒤 서버가 돌려주는 HP를 반영한다. 클라이언트의
   장부/실체 분리 구조(8.3절)는 그대로 쓸 수 있다 — 장부의 출처만 시드에서
   구독으로 바뀐다.
4. **다른 플레이어 렌더링** — `world_player`를 구독해 `IsoEntity` 컴포넌트로 그린다.
5. **PK** — `attack_player` reducer를 얹는다. 안전지대 판정(`in_safe_zone`)과
   체력·좌표는 서버에 이미 있다.

지형은 시드만 공유하면 모든 클라이언트가 같은 월드를 생성하므로 전송할 필요가
없다. 이것이 절차적 생성을 택한 실질적 이득이다.

---

## 14. 파일 지도

```
lib/
├─ main.dart                  앱 진입점 (온라인)
├─ offline_main.dart          서버 없이 게임만 실행
├─ preview_main.dart          캐릭터 렌더링 미리보기
│
├─ auth/                      계정 · 캐릭터 관문
│  ├─ cyborg_gate.dart          연결→로그인→선택→출격 흐름 제어
│  ├─ cyborg_session.dart       서버 view를 비추는 전역 컨트롤러
│  ├─ cyborg_kind.dart          서버 문자열 ↔ 신체 프레임
│  ├─ login_screen.dart / character_select_screen.dart
│  ├─ cyborg_portrait.dart / ui_kit.dart
│
├─ game/
│  ├─ action_rpg_game.dart    게임 본체 (FlameGame) — 스트리밍·입력·카메라
│  ├─ iso.dart                좌표 변환과 월드 상수
│  ├─ palette.dart            색 팔레트
│  │
│  ├─ level/
│  │  ├─ level_map.dart         절차적 지형 (Uint8List, 청크 조회)
│  │  ├─ ground_layer.dart      청크 Picture 캐싱 렌더러
│  │  └─ safe_zone.dart         안전지대 판정 + 역장 렌더링
│  │
│  ├─ entities/
│  │  ├─ iso_entity.dart        모든 월드 오브젝트의 베이스 (그리드↔화면)
│  │  ├─ player.dart            플레이어 상태 기계
│  │  ├─ cyborg_design.dart     체형 수치 정의
│  │  ├─ cyborg_renderer.dart   절차적 캐릭터 렌더러
│  │  ├─ enemy.dart             로봇 4종 + AI
│  │  ├─ block.dart             데이터 타워 / 캐시 (큐브)
│  │  ├─ projectile.dart        발사체
│  │  ├─ weapon_art.dart        무기 실루엣·표면 무늬·계통별 스윙
│  │  └─ pickup.dart            전리품 (낙하 물리 + 자석)
│  │
│  ├─ systems/
│  │  ├─ level_system.dart      경험치 곡선과 성장 규칙
│  │  ├─ camera_zoom.dart       시야 배율 단계 (화면 몫 × 사람이 고른 몫)
│  │  ├─ monster_population.dart 월드 전역 개체군 장부
│  │  ├─ drop_table.dart        기종별 드롭 표
│  │  ├─ elite.dart            정예 변종 (체력·속도·경보 반경, 피해는 불변)
│  │  ├─ enemy_tactics.dart    계통별 추격·후퇴 방향 (Flame 없이 시험 가능)
│  │  ├─ monster_codex.dart    200종 도감 · 구역 등급 곡선과 그 역함수
│  │  ├─ weapon.dart            무기 등급표·계통·5레벨 순환·위력 곡선·드롭 굴림
│  │  ├─ inventory.dart         포션 가방
│  │  └─ buff.dart              포션 버프
│  │
│  ├─ fx/                      데미지 텍스트 · 히트 스파크 · 폭발 · 전리품 라벨
│  ├─ audio/game_audio.dart    효과음 관리
│  ├─ net/                     GameSync 인터페이스 + SpacetimeDB 구현
│  └─ ui/                      HUD · 미니맵 · 메뉴 · 인벤토리 · 캐릭터 · 리더보드
│
└─ spacetime/
   ├─ cyborg_connection.dart   호스트·DB 이름, 토큰 저장소
   ├─ spacetime_leaderboard.dart
   └─ generated/               spacetime generate 산출물

spacetimedb/src/
├─ lib.rs                     테이블 · view · 라이프사이클
├─ auth.rs                    회원가입 · 로그인 · 해시
├─ character.rs               캐릭터 생성 · 선택 · 삭제
├─ leaderboard.rs             순위 계산 view · 경험치 곡선표(정본)
└─ world.rs                   서버 권위 몬스터 · 선점 킬 판정 · 이동 검증

assets/audio/                 효과음 · BGM
```

---

## 15. 밸런스 상수 요약

한곳에서 조정 가능한 값들. 괄호 안은 정의 위치.

| 영역 | 상수 |
|---|---|
| 월드 | 걸을 수 있는 1000×1000 타일 + 외곽 테두리 3 → 격자 1006×1006, 청크 32, 타일 128×64 px, 고도 단위 56 px (`iso.dart`) |
| 안전지대 | 50 m 정사각형, 월드 정중앙 (`safe_zone.dart`) |
| 어그로 | 1~5 m. 계통별 구간에서 개체마다 무작위, 추격 포기 = 어그로 +2.5 m (`monster_codex.dart`, `enemy.dart`) |
| 경보 연쇄 | 일반 4.5 m, 경계 지휘 정예 12 m (`elite.dart`) |
| 원거리 후퇴 | 사거리의 55%보다 가까우면 물러난다, 경직 이동 속도 45% (`enemy_tactics.dart`) |
| 정예 출현 | 시작 3.5% → 외곽 8.5%, 체력 ×1.7~2.6 (`elite.dart`) |
| 스트리밍 | 구조물 여유 16타일 / 0.25초, 로봇 활성 46 m · 해제 60 m · 상한 140기 / 0.3초, 지면 청크 캐시 96개 · 프레임당 3개 (`action_rpg_game.dart`, `ground_layer.dart`) |
| 플레이어 | HP 2,500(레벨당 +250), EN 100, 근접 26, 원거리 18, 속도 3.6, 방어력 0 (`player.dart`) |
| 근접 | 0.32초, 사거리 1.5, 부채꼴 120°, 3타 ×1.6, 콤보창 0.65초 |
| 대시 | 0.18초, 속도 13, 쿨 0.9초, EN 20 |
| 사격 | 쿨 0.24초, EN 12, 탄속 9.5 |
| 무적 | 피격 0.55초, 재가동 2초 |
| 적 | 레벨 1~200 도감 200종, **피해 = 레벨** (`monster_codex.dart`의 `MonsterCodex`) |
| 리스폰 | 75초 (`monster_population.dart`) |
| 레벨 | 만렙 30, `60×1.22^(n-1)`, 감소 하한 0.55 (`level_system.dart`) |
| 드롭 | 기종별 표 (`drop_table.dart`) |
| 무기 | 위력 `1+1.2×(1−0.99^(n-1))`, 등급 12단, 등급당 사거리 +0.04 (`weapon.dart`) |
| 무기 계통 | 블레이드·해머·랜스·리퍼·드라이버·탈론·볼텍스 7종. 초당 피해는 서로 5% 안쪽, 색상 hue 10·55·100·145·236·281 (`weapon.dart`, `weapon_art.dart`) |
| 기본 무기 계통 | **5레벨마다** 다음 계통으로 갈리고 7종을 다 돌면(35레벨) 처음으로 돌아온다 (`WeaponSystem.innateCycle`) |
| 무기 드롭 | 잡몹 1~4.5% · 보스 35%, 로봇 레벨 ±3, 벼림 ×0.90~×1.25 (`weapon.dart`, `drop_table.dart`) |
| 서버 | 해시 30,000회, 캐릭터 4개, 이름 2~16자, 리더보드 100위 |
| 서버 월드 | 몬스터 240기, 근접 사거리 2.2타일, 공격 쿨 0.35초, 선점 태그 수명 30초, 리스폰 20초, 정비 주기 5초, 이동 상한 14타일/초 (`world.rs`) |

---

## 부록. 개발 메모

**전량 절차적 아트를 택한 대가.** 에셋이 없어 반복 작업과 용량 문제에서 자유롭지만,
캐릭터 하나를 그리는 데 수백 줄의 Canvas 코드가 든다 (`cyborg_renderer.dart` 626줄).
대신 체형을 수치로 뽑아냈기 때문에 새 프레임 추가는 `CyborgDesign` 항목 하나면 된다.

**아이소메트릭에서 반복적으로 부딪힌 함정.**
- 키보드 입력을 그리드 방향으로 변환하지 않으면 조작이 45도 틀어진다
- 높은 구조물은 화면 경계 밖에서도 위로 솟아 보이므로 컬링 여유를 넉넉히 잡아야 한다
- 화면 사각형은 그리드 위에서 마름모다 — 네 모서리를 역변환해 감싸는 사각형을 쓴다
- `priority`가 정수라 깊이 정렬 시 100배해서 소수점을 보존해야 한다

**1 km 월드를 감당한 방법.** 데이터는 평면 배열, 실체는 플레이어 근처에만.
이 원칙이 지형·구조물·몬스터 세 계층에 모두 같은 형태로 적용되어 있다.
