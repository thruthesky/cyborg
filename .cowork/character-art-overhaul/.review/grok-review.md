## 리뷰 판정

종합본의 **핵심 진단(“병목은 아키텍처가 아니라 조형”)과 하지 말 것 목록은 코드·원본과 잘 맞고 유지할 가치가 크다.**  
가장 큰 결함은 **§3에서 blur를 “성능 주범”으로 단정한 뒤, §8에서는 같은 수치를 `[추측]`·미실측이라고 두는 내부 모순**이다. 그 단정이 권고 4의 높은 순위를 지탱하고 있어, 합의 문장만 완화해도 권고 우선순위 논리가 다시 짜여야 한다.  
그다음으로 **실제 최소 배율(0.275) 결정이 권고 5로 밀려**, 권고 1~4의 디테일·명암 규격이 목표 픽셀 없이 진행될 재작업 위험이 있다. 원본 고유 통찰의 대부분은 §5에 잘 살렸고, 환각성 `파일:줄`은 샘플 검증에서 대체로 확인됐다.

## 지적 사항

| # | 위치 | 종류 | 지적 | 반영 시 어떻게 |
|---|---|---|---|---|
| 1 | §3 · §7 권고 4 · §8 | 과장 / 논리 | §3 합의문이 **「blur가 성능 주범이고 이미 과하다」**로 단정한다. 그런데 §8은 blur·gradient ms가 셋 다 `[추측]`이고 프로파일이 없다고 명시한다. codex/grok/kimi 원본도 비용을 `[추정]`/`[추측]`으로만 적었다. **같은 보고서 안에서 합의(단정)와 미해결(추측)이 충돌**한다. 권고 4(blur 다이어트)의 높은 순위가 이 단정에 기대 있다. | §3을 「구조상 캐릭터·적당 다수 blur 호출이 있어 **성능 위험 후보**다. 주범 여부는 프로파일 전」으로 완화. 권고 4 검증란에 `flutter run --profile` / DevTools 30~50 엔티티 시나리오를 **선행 조건**으로 붙이거나, 순위 표기를 「조형 병행 · 우선순위는 실측 후 확정」으로 분리. |
| 2 | §7 권고 1~5 · §8 최소 배율 | 권고 / 논리 | codex가 강조한 **유효 최소 배율 0.275**(`clamp(0.55,1.6) * _minZoomScale(0.5)`)는 §5에 올바르게 실렸고 코드로 확인된다(`action_rpg_game.dart:321,350-353`). 그러나 권고 표에서는 **명암·베지에·헬멧·blur(1~4) 다음에** LOD/최소 배율(5)이 온다. 디테일 최소 픽셀·LOD 생략 규칙은 목표 화면 높이(약 30px vs 59px)에 종속되므로, **5를 먼저 정하지 않으면 1~4가 재작업**되기 쉽다. 스냅샷 주석도 여전히 “최소 0.55 / 59px”만 본다(`cyborg_render_snapshot_test.dart:17-19`). | 권고 순서를 **「최소 배율·LOD 목표 확정 → 조형/명암 → blur」**로 재배치하거나, 권고 1에 “목표 배율(0.275 유지 vs 0.55 하한) 결정을 게이트”로 명시. 검증에 **0.275 시트 추가 + 스냅샷 주석 수정**을 같은 항목에 묶을 것. |
| 3 | §3 · §7 성능 전제 | 누락 / 미검증 | 성능 예산을 **30명** 전제로 읽는 문맥이 강하다. 코드상 동시 표시 상한은 **원격 플레이어 50 · 활성 몬스터 50**(`action_rpg_game.dart:216-223`). kimi만 50을 근거로 썼고, 종합본이 그 상한을 합의·권고 예산에 거의 반영하지 않았다. blur “과다” 논의를 30명으로 잡으면 **실제 상한 대비 과소 추정**이 된다. | 합의/권고의 부하 시나리오를 **「로컬 1 + 원격 ≤50 + 몹 ≤50」**으로 명시. 30명은 “대표 혼잡”이면 그렇게 라벨링하고, 상한 시나리오와 구분. |
| 4 | §2 blur 행 · §3 · §7 권고 4 | 미검증 | 「렌더러 5개소·적 7개소」는 `cyborg_renderer.dart` / `enemy.dart` 내 `MaskFilter.blur` 정적 개소로 **맞다**. 다만 **실행 시 캐릭터당 blur**에는 `IsoEntity.renderShadow`의 blur 1회(`iso_entity.dart:55-68`)가 항상 더해지고, 플레이어 쪽 대시/이펙트 blur(`player.dart` 등)는 범위 밖 처리다. codex는 “본체+그림자 3~6”으로 더 넓게 잡았다. 종합본이 “5→2” 목표만 쓰면 **그림자·전투 FX를 빼 먹은 예산**이 된다. | “렌더러 파일 내 개소”와 “프레임당 실제 blur 페인트”를 구분. 다이어트 목표에 **그림자 blur 유지/통합 여부**를 한 줄 추가. 캐릭터당 목표는 `drawBody`만이 아니라 그림자 포함 여부를 명시. |
| 5 | §1 · §7 권고 1 | 과장 | 사용자 불만의 핵심 표현은 **「동그라미·네모를 붙인 형태」**(실루엣·곡률·사지 프리미티브)다. 종합본이 Gradient를 **「가장 값싸고 효과가 큰 수단」**으로 격상한 것은 kimi의 1순위와는 맞지만, codex/grok는 **조형(베지에·골격)과 명암을 한 묶음**으로 1순위에 둔다. Gradient는 평면감 완화에 유리하나, “레고/네모” 인상의 **1차 해결책이라고 단정할 근거는 약하다**(스냅샷 육안 검증도 전 AI 미실시). | 권고 1을 「명암 3~4단(Gradient 포함)」으로 유지하되, 표현을 **「저비용 개선 후보」**로 낮추고, **실루엣 베지에(현 2)와 동급 1차 패키지**로 묶거나 “형태 불만 해소의 주축은 2·3, 1은 재질감”으로 역할을 분리. |
| 6 | §4 bake · §7 권고 11 vs grok 원본 권고 5 | 반증 | grok은 **인게임 quantizeYaw + Picture/Image bake를 권고 5**로 올렸다. 종합본은 메모리 가정 차이를 §4에서 잘 정리하고 권고 11로 뒤로 밀었으나, **「grok의 조기 bake 채택은 기각」**을 이견/반증 문장으로 명시하지 않아, 종합만 읽으면 grok 원본과 충돌이 남을 수 있다. | §4 bake 절 말미에 한 문장: **「1차 품질 수단으로 bake를 올리는 안(grok 권고 5)은 채택하지 않는다. 프로파일 후 성능 수단만」**. |
| 7 | §5 · §7 권고 2 | 누락 | codex 고유에 가까운 **「팔꿈치 없이 어깨→손 사각 기둥, 보행 시 끝점만 이동 → 막대 다리」**(`cyborg_renderer.dart:145-178` 등)가 결론·권고에서 **실루엣 베지에·테이퍼**로만 흡수된다. 주석 자체도 무릎 마디가 없으면 막대로 뭉친다고 인정한다(`:165-166`). 정적 외곽 곡선만으로는 **동적 관절 분절 부족**이 남을 수 있고, 그건 Rive(권고 12) 전에 **코드 분절 Path**로도 가능하다는 codex 취지가 흐려진다. | 권고 2 또는 별도 bullet에 **「상완/전완·허벅지/종아리 분절(관절에서 꺾이는 2-Path)」**을 명시. “볼륨 곡선”과 “관절 각도”를 구분해, Rive 이전의 코드 상한을 분명히. |
| 8 | §7 권고 7~10 | 권고 | 「조형이 파츠보다 먼저」는 옳다. 다만 최종 권고가 **1~12가 한 표에 병렬**이라, 구현자가 파츠·서버 영속화(7~10)를 조형과 동시에 집을 위험이 있다. 권고 10(SpacetimeDB 외형 필드)은 MMORPG상 맞지만, **“예쁘게” 1차 범위 밖으로 게이트**하지 않으면 스키마·원격 동기 범위가 팽창한다. `remote_player`의 `kind == 'female_cyborg'`뿐 아니라 `leaderboard_screen.dart:514` 등 **문자열 직접 분기 잔여**도 원본 대비 약하게만 다뤄졌다. | 표를 **Phase A(조형·LOD·blur)** / **Phase B(파츠·커스터마이저)** / **Phase C(서버 영속·resolver 통일)** 로 나누고, B 착수 조건을 “A 스냅샷 합격”으로. 권고 10에 kind 문자열 분기 전수 정리 대상을 remote 외 1~2곳 더 적시. |
| 9 | §1 · §3 Gradient 0회 | 유지 가능하나 표현 | `cyborg_renderer.dart`·`enemy.dart`에서 Gradient 0건, 헬멧 `Offset(0, cy)`, `neckLength` 미사용, `copyWith` 부재, `MonsterCodex.maxLevel = 200` vs `switch(build)` 4분기, `quantizeYaw` 호출 0 — **직접 대조 시 모두 성립**. 문제는 사실이 아니라 일부 문장의 **확실도 라벨**이다. | 사실 주장은 유지. “성능 주범/가장 효과” 같은 **평가 문장에만** `[판단]`/`[추측]`을 붙일 것. |

## 유지해야 할 강점

- **「Spec → 공유 즉시모드 렌더러」가 이미 네 표면(player / remote / portrait / preview)에 있다**는 판정 — 아키텍처 재발명 차단이 정확하다.
- **정적 LPC 스프라이트 반대 + LPC에서 가져올 것은 슬롯·레이어 순서** — 요구·라이선스·기존 코드와 정합.
- **§6 반증**(CustomPainterComponent, 파츠 PositionComponent 트리, `priority = y`, Y 기반 크기 원근, 글로우 추가) — 잘못된 설계 유혹을 근거와 함께 잘 차단했다.
- **몬스터 1차 연속 yaw 보류(kimi 판정 채택)** — 텔레그래프·조준선·플립 비대칭과 묶인 게임플레이 신호라는 논리가 타당하다.
- **§5 고유 통찰 표**(실효 최소 0.275, 헬멧 yaw 무관, `neckLength` 미사용, `copyWith` 부재, remote `kind` 분기, 공통 세로 비율, 문서 626 vs 실제 994줄) — 다수결로 버리지 않았고 샘플 코드 검증과 일치한다.
- **「조형 먼저, 파츠 나중 / 레이어 시스템 ≠ 예쁨」** — grok 통찰을 결론·권고 철학으로 잘 고정했다.
- **bake·Rive를 품질 1순위가 아닌 후순위·프로파일 후로 둔 방향** — 메모리·연속 yaw 퇴보 논거와 맞다(다만 grok 조기 bake 기각 문구만 보강).

## 확신도

- **높음(코드 직접 확인):** Gradient 부재, blur 정적 개소 5/7, 헬멧 `Offset(0,cy)`, `neckLength` 미사용, `CyborgDesign.copyWith` 없음, zoom `0.55×0.5=0.275`, 원격/몹 상한 50, `maxLevel=200` vs 렌더 4분기, `remote_player` kind 분기, `quantizeYaw` 정의만 존재, GAME-DESIGN 626줄 기재 vs 렌더러 994줄, 스냅샷 0.55만 검사.
- **중간(논리·우선순위 판단):** blur를 주범으로 단정한 과장, 최소 배율을 권고 5로 둔 순서, Gradient “최대 효과” 격상, Phase 분리 권고 — 원본 간 이견·미실측을 종합한 **리뷰어 판단**.
- **[추측] 아님을 명시:** 실제 프레임 ms, Gradient 적용 후 체감 “예쁨”, 스냅샷 PNG 육안 순위 — 이번 리뷰에서도 프로파일·이미지 실행은 하지 않았다. 그 부분은 종합본 §8과 동일하게 열린 문제로 둔다.
